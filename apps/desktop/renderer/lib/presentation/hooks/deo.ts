'use client';

/** DEO portal — no ViewModels of its own yet; every screen under
 * app/government/deo is still a placeholder. Add DEO-owned hooks here, the
 * same way school-admin.ts and teacher.ts are organized, once real
 * ViewModels exist for this portal. */
export {};
