'use client';

/** County (CEO) portal — no ViewModels of its own yet; every screen under
 * app/government/county is still a placeholder. Add county-owned hooks here,
 * the same way school-admin.ts and teacher.ts are organized, once real
 * ViewModels exist for this portal. */
export {};
