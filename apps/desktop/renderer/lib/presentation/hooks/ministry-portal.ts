'use client';

/** Ministry portal — no ViewModels of its own yet; every screen under
 * app/government/ministry-portal is still a placeholder. Add ministry-owned
 * hooks here, the same way school-admin.ts and teacher.ts are organized,
 * once real ViewModels exist for this portal. */
export {};
