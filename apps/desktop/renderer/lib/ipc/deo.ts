/** DEO portal — the ApplicationLayer contract (packages/application) has no
 * DEO-specific service keys yet, so there is nothing to compose here. Once
 * DEO IPC endpoints exist, add the ApplicationLayer group(s) here and merge
 * them into lib/ipc/index.ts, the same way school-admin.ts and teacher.ts
 * are wired in. */
export {};
