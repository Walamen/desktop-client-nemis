import { ScopedPortalDashboard } from '@/components/dashboard/ScopedPortalDashboard';
export default function Page() {
  return <ScopedPortalDashboard title="Teacher portal" description="Only your assigned classes, subjects, learners, attendance, and timetable are stored here." />;
}
