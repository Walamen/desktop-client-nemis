import { ScopedPortalDashboard } from '@/components/dashboard/ScopedPortalDashboard';
export default function Page() {
  return <ScopedPortalDashboard title="Ministry portal" description="Nationally authorized education data are available in this encrypted offline workspace." />;
}
