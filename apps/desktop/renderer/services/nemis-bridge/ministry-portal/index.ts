/** Ministry portal — no live IPC endpoints yet; every screen under
 * app/government/ministry-portal is still a placeholder. Add ministry-
 * specific bridge calls here as their own file, the same way school-admin/
 * and teacher/ are organized, once the Electron main process exposes them on
 * window.nemis. */
export const ministryPortalBridge = {};
