/** Ministry portal — no NemisApi channels of its own yet. Add
 * ministry-specific preload API pieces here, the same way school-admin/ and
 * teacher/ are organized, once new IPC channels exist for this portal. */
export {};
